---
- hosts: all
  gather_facts: no
  vars_files: arch-vars-file.yaml
  roles:
    - pacman-keyring
    - python
    - upgrade-packages
    - install-packages
    - download-dotfiles-repo
    - create-user-bitcoin
    - create-user-lnd
    - mount-drive
    - config-bitcoin
    - config-tor
    - install-lnd
    - config-lnd
    - fix-permissions
    - enable-systemd-units
